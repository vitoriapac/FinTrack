const {test,expect}=require('@playwright/test');
test.beforeEach(async({page})=>{await page.goto('/');await page.evaluate(()=>localStorage.clear());await page.reload();if(await page.locator('#nav-mobile-toggle').isVisible())await page.locator('#nav-mobile-toggle').click();});
for(const [view,title] of [['Patrimônio','Composição patrimonial'],['Metas','Progresso das metas'],['Planejamento','Orçado, realizado e pendente'],['Dívidas','Evolução do saldo devedor'],['Histórico','Resultados históricos']])test(`${view} mantém gráfico e detalhamento auditável`,async({page})=>{await page.getByRole('button',{name:view,exact:true}).click();await expect(page.locator('.chart').filter({hasText:title})).toBeVisible();await expect(page.getByRole('heading',{name:new RegExp(view)}).first()).toBeVisible();});
