const {test,expect}=require('@playwright/test');
test('central reúne análises determinísticas sem erros',async({page})=>{const errors=[];page.on('pageerror',error=>errors.push(error.message));await page.goto('/');await page.evaluate(()=>localStorage.clear());await page.reload();if(await page.locator('#nav-mobile-toggle').isVisible())await page.locator('#nav-mobile-toggle').click();await page.locator('.nav-group-toggle').filter({hasText:'Analisar'}).click();await page.getByRole('button',{name:'Análises',exact:true}).click();await expect(page.getByRole('heading',{name:'Análises'})).toBeVisible();for(const title of ['Comprometimento futuro','Recorrências','Reserva de emergência','Qualidade da projeção','Mês atípico','Concentração de gastos'])await expect(page.getByText(title,{exact:true}).first()).toBeVisible();expect(errors).toEqual([]);});
