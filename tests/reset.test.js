const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),context={window:{}};vm.createContext(context);vm.runInContext(fs.readFileSync('js/services/reset.js','utf8'),context);const current={usuario:{nome:'Ana',onboardingIgnorado:true,preferencias:{compacto:true}},contas:[{}],categorias:[{}],lancamentos:[{}],fechamentos:{x:{}},planejamentos:{x:{}},_backups:[{}],modoDemo:true};const next=context.window.DataResetService.createEmptyState(current);assert.equal(next.usuario.nome,'Ana');assert.equal(next.usuario.onboardingIgnorado,true);assert.deepEqual(JSON.parse(JSON.stringify(next.usuario.preferencias)),{compacto:true});assert.equal(next.contas.length,0);assert.equal(next.categorias.length,0);assert.deepEqual(JSON.parse(JSON.stringify(next.fechamentos)),{});assert.equal(next._backups.length,0);assert.equal(next.modoDemo,undefined);console.log('reset tests: OK');
