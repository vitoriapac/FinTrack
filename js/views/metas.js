(function(){
  window.renderMetasView=function(){const summaries=(state.metas||[]).map(goal=>({goal,value:GoalService.summary(state,goal,todayLocal())})),chart=FinTrackCharts.horizontalBars({title:'Progresso das metas',question:'Quanto já foi acumulado?',summary:'O valor restante e o aporte médio aparecem em cada meta.',data:summaries.map(({goal,value})=>({label:goal.nome,value:goal.alvo?Math.round(value.acumulado/goal.alvo*100):0})),format:value=>`${value}%`,empty:{message:'Crie uma meta para acompanhar sua evolução.',action:{label:'Criar meta',target:'metas'}}}),content=summaries.length?summaries.map(({goal,value})=>{const status={no_ritmo:'Normal',atrasada:'Atenção',adiantada:'Normal',concluida:'Normal'}[value.status],cards=[MetricCard.render({label:'Acumulado',value:formatMoney(value.acumulado),tone:'positive'}),MetricCard.render({label:'Restante',value:formatMoney(Math.max(0,goal.alvo-value.acumulado))}),MetricCard.render({label:'Aporte médio',value:formatMoney(value.aporteMedio)}),MetricCard.render({label:'Previsão',value:value.status==='concluida'?'Concluída':value.previsaoConclusao||'Sem base',tone:value.status==='atrasada'?'warning':'neutral'})].join('');return DataPanel.render({title:goal.nome,question:`Estado: ${status}`,state:value.status==='atrasada'?'atencao':'normal',action:{label:'Excluir',target:`delete-goal:${goal.id}`},content:`<div class="metric-grid cards-row">${cards}</div><p class="data-panel-summary">${value.contribuicoes.length} contribuição(ões) vinculada(s) · prazo ${formatDate(goal.prazo)}</p>`});}).join(''):emptyState('Nenhuma meta cadastrada','Crie sua primeira meta financeira.');return `<div class="page-header"><div class="page-heading"><h1 class="page-title">Metas financeiras</h1><p class="page-sub">Acumulado derivado de contribuições e estornos</p></div><div class="page-actions"><button class="btn btn-primary" id="btn-nova-meta">Nova meta</button></div></div><div class="dashboard-grid">${chart}</div>${content}`;};
  FinTrackViews.register('metas',()=>window.renderMetasView());
})();
