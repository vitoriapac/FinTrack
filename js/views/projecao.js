(function(){
  window.renderProjecaoView=function(){
    const result=ProjectionService.project(state,mesAtualKey(),3),rows=result.meses,chart=FinTrackCharts.area({title:'Saldo projetado',question:'Como confirmado e planejado afetam o saldo?',summary:'Cenário consolidado sem duplicar compromissos já materializados.',data:rows.map(item=>({label:item.mes,value:item.projetado.saldoFinal}))});
    return `<div class="page-header"><div class="page-heading"><h1 class="page-title">Projeção financeira</h1><p class="page-sub">Confirmado, planejado e projetado nos próximos três meses</p></div><button class="btn btn-ghost" id="btn-cenario-projecao">Simular cenário</button></div><div class="dashboard-grid">${chart}</div><div class="section"><div class="section-head"><h2>Fluxo projetado</h2><span class="stat-meta">Não altera dados reais</span></div><div class="table-wrap"><table><thead><tr><th rowspan="2">Mês</th><th colspan="3">Confirmado</th><th colspan="3">Planejado</th><th colspan="2">Projetado</th><th rowspan="2">Atenção</th></tr><tr><th class="num">Entradas</th><th class="num">Saídas</th><th class="num">Invest.</th><th class="num">Entradas</th><th class="num">Saídas</th><th class="num">Invest.</th><th class="num">Menor saldo</th><th class="num">Saldo final</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${row.mes}</td><td class="num money-in">${formatMoney(row.confirmado.entradas)}</td><td class="num money-out">${formatMoney(row.confirmado.saidas)}</td><td class="num">${formatMoney(row.confirmado.investimentos)}</td><td class="num money-in">${formatMoney(row.planejado.entradas)}</td><td class="num money-out">${formatMoney(row.planejado.saidas)}</td><td class="num">${formatMoney(row.planejado.investimentos)}</td><td class="num ${row.projetado.menorSaldo<0?'money-out':''}">${formatMoney(row.projetado.menorSaldo)}</td><td class="num ${row.projetado.saldoFinal<0?'money-out':''}">${formatMoney(row.projetado.saldoFinal)}</td><td>${row.alerta?`<span class="badge badge-pending">${row.alerta.replace('_',' ')} · ${esc(row.principalImpacto||'')}</span>`:'—'}${row.excessoOrcamento?`<small class="money-out">Excesso: ${formatMoney(row.excessoOrcamento)}</small>`:''}</td></tr>`).join('')}</tbody></table></div></div><div class="section"><div class="section-head"><h2>Referência diária</h2></div><div class="history-note"><strong>${formatMoney(result.referenciaDiaria)} por dia</strong><p>${esc(result.aviso)}</p></div></div>`;
  };
  const qualityProjection=window.renderProjecaoView;window.renderProjecaoView=function(){const quality=AnalysisService.dataQuality(state,{startMonth:mesAtualKey(),months:3}),labels={normal:'Alta',atencao:'Média',critico:'Baixa'},panel=DataPanel.render({title:'Qualidade da projeção',question:'Quanto posso confiar nesta previsão?',state:quality.status,summary:`${quality.score}/100 · ${labels[quality.status]||'Indisponível'}`,content:`<div class="metric-grid">${quality.metrics.map(item=>MetricCard.render({label:item.month,value:`${item.score}%`,tone:item.score<60?'negative':'neutral',detail:item.estimated?'Inclui valores estimados':'Base conhecida'})).join('')}</div><details><summary>Por que não é 100?</summary><p>Obrigações ${quality.evidence.dimensions.coverage}/35 · receita ${quality.evidence.dimensions.income}/25 · completude ${quality.evidence.dimensions.completeness}/20 · histórico ${quality.evidence.dimensions.history}/10 · atualização ${quality.evidence.dimensions.updates}/10.</p></details>`});return qualityProjection()+panel;};
  FinTrackViews.register('projecao',()=>window.renderProjecaoView());
})();
