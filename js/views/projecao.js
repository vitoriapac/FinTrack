(function(){
  window.renderProjecaoView=function(){
    const result=ProjectionService.project(state,mesAtualKey(),3),rows=result.meses;
    return `<div class="page-header"><div class="page-heading"><h1 class="page-title">Projeção financeira</h1><p class="page-sub">Cenário-base para os próximos três meses</p></div><button class="btn btn-ghost" id="btn-cenario-projecao">Simular cenário</button></div><div class="section"><div class="section-head"><h2>Fluxo projetado</h2><span class="stat-meta">Não altera dados reais</span></div><table><thead><tr><th>Mês</th><th class="num">Entradas</th><th class="num">Saídas</th><th class="num">Investimentos</th><th class="num">Menor saldo</th><th class="num">Saldo final</th><th>Atenção</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${row.mes}</td><td class="num money-in">${formatMoney(row.entradas)}</td><td class="num money-out">${formatMoney(row.saidas)}</td><td class="num">${formatMoney(row.investimentos)}</td><td class="num ${row.menorSaldo<0?'money-out':''}">${formatMoney(row.menorSaldo)}</td><td class="num ${row.saldoFinal<0?'money-out':''}">${formatMoney(row.saldoFinal)}</td><td>${row.alerta?`<span class="badge badge-pending">${row.alerta.replace('_',' ')} · ${esc(row.principalImpacto||'')}</span>`:'—'}</td></tr>`).join('')}</tbody></table></div><div class="section"><div class="section-head"><h2>Referência diária</h2></div><div class="history-note"><strong>${formatMoney(result.referenciaDiaria)} por dia</strong><p>${esc(result.aviso)}</p></div></div>`;
  };
  FinTrackViews.register('projecao',()=>window.renderProjecaoView());
})();
